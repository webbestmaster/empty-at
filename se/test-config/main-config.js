const mainConfig = {
    _url: { // eslint-disable-line id-match
        host: 'https://www.gi-test6.marathonbet.co.uk',
        pathname: '/en/toto/gi'
    },
    url: { // eslint-disable-line id-match
        host: 'http://localhost:8181',
        pathname: '/#'
    }
};

module.exports.mainConfig = mainConfig;
